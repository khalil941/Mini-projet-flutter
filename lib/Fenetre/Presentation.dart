import 'package:flutter/material.dart';

class PresnetationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Dashboard Page"),
      ),
    );
  }}